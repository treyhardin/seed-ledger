# Copy to .env to override defaults (used by `npm start` and docker compose).

# Port the app listens on (UI + API).
PORT=3000

# Folder that holds garden.db. Defaults to the project root.
# Ignored under Docker, where data lives in the seed-ledger-data volume.
# DATA_DIR=./data
